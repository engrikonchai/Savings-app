# CarGoal — Web Preview (auto-generated)

This branch contains a static export of the CarGoal app for GitHub Pages.
It is generated output, not source — see the `claude/cargoal-ios-app-jsa7yp`
branch (or `main`) for the actual app source code.
